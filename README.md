This repo stores a JavaScript file in which I wrote higher-order 
functions such as map, filter, reduce, and sort. 

The functions are stored in worksheet.js and are tested using the 
checkWork.sh file.
